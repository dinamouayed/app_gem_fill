# Themes catalog — add entries here, not in Python code.
#
# Each theme maps a display name to a procedural pattern id.
# Available patterns: run
#   npm run levels -- patterns
#
# Example batch file (themes/objects.json):
# {
#   "themes": [
#     { "name": "Avocat", "pattern": "heart" },
#     { "name": "Ours", "pattern": "cat" }
#   ]
# }
#
# Generate:
#   npm run levels -- themes themes/objects.json --start-id 61
